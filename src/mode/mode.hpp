#ifndef MODE_HPP
#define MODE_HPP

class DataNode
{
private:
    /* data */
public:
    DataNode(/* args */);
    ~DataNode();
};

// ================================================================

class MasterNode : public DataNode
{
private:
    /* data */
public:
    MasterNode(/* args */);
    ~MasterNode();
};

// ================================================================

class DNS
{
private:
    /* data */
public:
    DNS(/* args */);
    ~DNS();
};

#endif

// ================================================================

class Client
{
private:
    /* data */
public:
    Client(/* args */);
    ~Client();
};
